import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    Main:{
        backgroundColor: '#000',
    }
})

export default styles

//O estilo deve ter o nome de styles, todo componente de estilo tem esse nome, a ultima coisa desse arquivo, deve ser a exportação padrão do stylesheet
//Deve ocorrer uma importação do StyleSheet, da lib react-native